# SkillArk v0.1 项目启动 · 开发计划

## 任务拆分

| # | 任务 | 依赖 | 验收点 |
|---:|---|---|---|
| 0 | 冻结 v0.1 范围、DTO、状态机和 SQLite 访问方案 | 无 | 设计 issue 的 5 项均进入 ADR/Schema；契约测试可运行 |
| 1 | 初始化 Tauri 2 + React/TypeScript + Rust 工程与质量门禁 | 0 中不争议部分 | 应用可启动；前后端测试、格式化和静态检查命令可运行 |
| 2 | 建立 Domain、Application、Ports 目录与核心类型 | 0 | Domain 不依赖 Tauri/IO；核心类型单测通过 |
| 3 | 实现 DetectionContext、信号采集和 Adapter 注册表 | 2 | 扫描可取消；未安装 Agent 不报错；总耗时 < 3 秒 |
| 4 | 实现 Claude Code、Cursor、Codex、WorkBuddy、Custom Adapter POC | 3 | 至少 3 个 Agent 得到可确认 Skill 路径；人工设置优先 |
| 5 | 实现 SKILL.md 解析、路径规则和稳定目录 Hash | 2 | 固定夹具跨重复运行 Hash 一致；逃逸链接被拒绝 |
| 6 | 落地 SQLite migration 与 repositories | 0、2 | migration 可在空库运行；repository 集成测试通过 |
| 7 | 实现本地目录和安全 ZIP 导入 | 5、6 | 合法 Skill 形成不可变版本；Zip Slip 夹具被拒绝 |
| 8 | 实现 DeploymentPlan 与冲突分类 | 2、4、6 | 每个冲突类型有单测；高风险目标要求确认 |
| 9 | 实现 Copy Driver 与补偿式事务 | 5、8 | 故障注入不破坏原目标；无不可识别临时目录 |
| 10 | 实现 Junction Driver、验证和卸载 | 8、9 | 安装/验证/卸载通过；修改目标不会静默删除 |
| 11 | 实现多目标执行和 Operation 审计 | 9、10 | 单目标失败不隐藏其他结果；操作可追溯 |
| 12 | 实现首次启动、Library、Agent、Workspace、分发向导 UI | 4、7、11 | loading/empty/ready/partial error/blocking error/stale 均有状态 |
| 13 | Windows 真机回归、安装包、迁移备份与发布门禁 | 12 | 两台设备、20 个 Skill、核心 E2E 与安全门禁通过 |

## 阶段顺序

```mermaid
flowchart LR
  M0["M0 设计冻结"] --> M1["M1 Agent 探测 POC"]
  M1 --> M2["M2 导入与分发 POC"]
  M2 --> M3["M3 MVP UI"]
  M3 --> M4["M4 发布准备"]
```

## 当前进度

| 任务 | 状态 | 结果 |
|---|---|---|
| 0 设计冻结 | 已完成 | 5 项差异进入 ADR、Schema 和契约测试 |
| 1 工程与质量门禁 | 已完成 | 前端构建、8 项 Rust 测试、Clippy 严格模式和 Tauri debug build 均通过 |
| 2 Domain/Application/Ports | 已初始化 | 核心目录和首批类型已建立 |
| 3 DetectionContext/信号/注册表 | 已完成 | 可取消扫描、未安装降级、原生进程快照；真实扫描 0.19 秒 |
| 4 Agent Adapter POC | 进行中 | Claude Code、Cursor、Codex、WorkBuddy 已实现；Custom 与完整验证矩阵待补 |

## 下一执行批次

1. 实现 Custom Adapter 与持久化人工路径设置。
2. 为四个内置 Agent 补默认/自定义/只读/中文路径/多实例验证矩阵。
3. 将 M1 探测 POC 收敛到独立 Agents 页面并展示信号明细。
4. M1 验收后进入 SKILL.md 解析、SQLite repositories 与安全导入。

## 进度更新（20260727 · M2 v0.1 核心）

| 任务 | 状态 | 结果 |
|---|---|---|
| 5 SKILL.md 解析 + 目录 Hash | 已完成 | 重复运行 Hash 一致；逃逸链被跳过 |
| 6 SQLite migration + repositories | 已完成 | 空 DB 可跑；仓库集成测试 8 项通过 |
| 7 本地目录 + 安全 ZIP 导入 | 已完成 | 内容寻址快照 + 版本去重；Zip Slip 夹具被拒（e2e 单测） |
| 8 DeploymentPlan + 冲突分类 | 已完成 | 8 种 `ConflictKind` 各有单测；高风险要求确认 |
| 9 Copy Driver + 补偿事务 | 已完成 | hash 不匹配不破坏目标；故障注入（stale 清理 + 失败回滚）单测 |
| 10 Junction Driver + 验证 + 卸载 | 已完成（环境受限） | mklink/rmdir 实现；创建测试因 EDR 在 test 进程被拦标 `#[ignore]`，交互式 shell 验证通过 |
| 11 多目标执行 + Operation 审计 | 已完成 | 单目标失败不影响其他；operation 可追溯（e2e 单测覆盖闭环） |
| 12 完整 MVP UI | 已完成 | Library / Deploy 向导（全局 + 项目范围）/ Workspaces / Operations / Agents 五页 + 首次启动向导全部落地 |
| 13 发布准备 | 部分完成 | DB `VACUUM INTO` 备份 + 单测；debug 构建通过；真机回归（两台 Windows 真机）与安装包签名待补（依赖物理设备与证书） |

详见 `logs/202607/20260727-m2-v0.1-core-import-deploy-loop.md`。
