# SkillArk v0.8 架构决策记录

> ADR 只有在证据变化或假设被证伪时重新打开，不因实现方便而绕过。

## ADR-0801

**决策：** 第三方扩展默认进程外运行，不加载任意动态库到主进程。

**原因：** 满足本版本的不可约简用户结果，同时保留失败降级和后续替换能力。

**反例/代价：** 该决策不是普遍最佳；若对应 H 假设失败，应按证伪结果重新打开 ADR。

**验证：** 通过本版本 POC、故障注入和发布门禁。

## ADR-0802

**决策：** 权限按能力授予，不按“完全信任”二元开关。

**原因：** 满足本版本的不可约简用户结果，同时保留失败降级和后续替换能力。

**反例/代价：** 该决策不是普遍最佳；若对应 H 假设失败，应按证伪结果重新打开 ADR。

**验证：** 通过本版本 POC、故障注入和发布门禁。

## ADR-0803

**决策：** 核心功能至少有一个官方内置实现，不依赖第三方扩展。

**原因：** 满足本版本的不可约简用户结果，同时保留失败降级和后续替换能力。

**反例/代价：** 该决策不是普遍最佳；若对应 H 假设失败，应按证伪结果重新打开 ADR。

**验证：** 通过本版本 POC、故障注入和发布门禁。

## 开放决策

- 所有 U 未知项在版本 POC 结束前必须关闭、降级为明确限制或推迟版本。
- 任何新公共接口在 v1.0 前默认 experimental，除非有真实调用方和兼容测试。
